name: Passenger H5 CI

on:
  push:
    branches:
      - feat/phase2-passenger-h5
    paths:
      - 'passenger-h5/**'
      - '.github/workflows/passenger-h5-ci.yml'
  pull_request:
    paths:
      - 'passenger-h5/**'
      - '.github/workflows/passenger-h5-ci.yml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: passenger-h5
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.19.0

      - name: Set up Node 22
        uses: actions/setup-node@v7
        with:
          node-version: '22.12.0'
          cache: pnpm
          cache-dependency-path: '**/pnpm-lock.yaml'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck and build
        run: pnpm run build
